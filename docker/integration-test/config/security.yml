# Fixed keys for integration testing — do NOT use in production.
production: &default
  encryption_key: <%= ENV.fetch('ENCRYPTION_KEY', 'e2e-encryption-key-32chars-here!!') %>
  jwt_encryption_keys:
    - 68ddd5576efad4bc90eed3ab0543a1112b0f51ec9425573a80a96cbc4a9e12b6
  lti_iss: <%= ENV.fetch('CANVAS_LTI_ISS', 'http://localhost:8900') %>

development:
  <<: *default

test:
  <<: *default
